{{- if $addressbook := .Values.addressbook }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: addressbook
  namespace: {{ .Values.target_namespace | default .Release.Namespace }}
data:
  addressbook: |
    {{- toYaml $addressbook | nindent 4 }}
{{- end }}
