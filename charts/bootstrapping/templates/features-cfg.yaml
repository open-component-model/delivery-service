{{- if $featuresCfg := .Values.featuresCfg }}
apiVersion: v1
kind: Secret
metadata:
  name: features-cfg
type: Opaque
stringData:
  features_cfg: |
    {{- toYaml $featuresCfg | nindent 4 }}
{{- end }}
