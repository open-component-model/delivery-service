name: Create Hotfix Branch
on:
  workflow_dispatch:
    inputs:
      odg-core-version:
        type: string
        required: true
        description: |
          ODG Core version to create hotfix branch for (`major.minor`)

jobs:
  create-hotfix-branch:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
      - uses: gardener/cc-utils/.github/actions/trusted-checkout@v1
        with:
          token-server: ${{ vars.FEDERATED_GITHUB_ACCESS_TOKEN_SERVER }}
          github-auth-token-permissions: |
            contents: write
            workflows: write
          init-git-identity: true
      - name: Create and push hotfix branch
        shell: bash
        env:
          ODG_CORE_VERSION: ${{ inputs.odg-core-version }}
        run: |
          odg_core_version="$ODG_CORE_VERSION"

          if [[ ! "$odg_core_version" =~ ^[0-9]+\.[0-9]+$ ]]; then
            echo "Error: odg-core-version must adhere 'major.minor' format"
            exit 1
          fi

          git fetch origin "refs/tags/${odg_core_version}.0:refs/tags/${odg_core_version}.0"
          git checkout "${odg_core_version}.0"

          next_version="${odg_core_version}.1-dev"
          echo "$next_version" | .ci/write-version
          git commit --all -m "next version: ${next_version}"

          git push origin "@:refs/heads/hotfix-$odg_core_version"
