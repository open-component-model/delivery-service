name: CI (non-release) (BDBA)
on:
  pull_request_target:
  push:
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build-bdba.yaml
    permissions:
      contents: read
      id-token: write
    with:
      mode: snapshot
